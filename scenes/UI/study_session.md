# study_session.gd (StudySession)

> **路径**: `res://scenes/ui/study_session.gd`
> **继承**: `Control`
> **类型**: UI层

## 概述
学习会话界面，负责展示当前卡片、学习进度、计时器等信息，监听 `StudyManager` 信号驱动 UI 更新。

> **当前状态**: 占位空壳，`_ready()` 中无实现逻辑。待后续接入 `StudyManager` 信号后填充。

## 公共方法
无。

## 信号
无。

## 常量
无。
