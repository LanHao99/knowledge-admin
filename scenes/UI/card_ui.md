# card_ui.gd (CardUI)

> **路径**: `res://scenes/ui/card_ui.gd`
> **继承**: `Control`
> **类型**: UI层

## 概述

卡片渲染控件，负责显示单张卡片的正反面内容并提供翻面/评分按钮交互。

> **当前状态**: 占位空壳，`_ready()` 中无实现逻辑。待后续接入 `CardManager` 与 `NoteManager` 后填充。

## 公共方法

无。

## 信号

无。

## 常量

无。
