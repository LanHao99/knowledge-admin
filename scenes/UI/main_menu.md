# main_menu.gd (MainMenu)

> **路径**: `res://scenes/ui/main_menu.gd`
> **继承**: `Node2D`
> **类型**: UI层 — 入口场景

## 概述
主菜单界面，作为程序启动入口，提供进入牌组列表、笔记列表、开始学习三个功能的入口按钮。

> **当前状态**: 占位空壳，`_ready()` 与 `_process()` 中均无实现逻辑。建议将 `project.godot` 的 `run/main_scene` 指向 `main_menu.tscn`。

## 公共方法
无。

## 信号
无。

## 常量
无。
