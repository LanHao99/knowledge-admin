# app.gd (App)

> **路径**: `res://src/app.gd`
> **继承**: `Node`
> **类型**: 全局单例 (Autoload)

## 概述
全局 Autoload 单例，承载跨场景的全局初始化逻辑（如数据库初始化、全局事件总线等）。

> **当前状态**: 占位空壳，仅声明 `extends Node`，无任何方法实现。待后续确定全局初始化策略后填充。

## 公共方法
无。

## 信号
无。

## 常量
无。
