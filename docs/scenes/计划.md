# Knowledge Admin — 项目计划

> 本文档记录项目的当前状态、已完成功能和下一步进展建议。

## 一、项目状态总览（2026-06-03）

### 架构完成度

| 层级       | 状态            | 说明                                                                                     |
| -------- | ------------- | -------------------------------------------------------------------------------------- |
| **UI 层** | 7/7 场景        | main_menu / deck_list / note_list / study / study_session / card_ui / debug_crud_panel |
| **逻辑层**  | 5/5 Manager   | DeckManager / NoteManager / CardManager / StudyManager / Manager 基类                    |
| **调度层**  | 3/3 Scheduler | Scheduler (基类) + SimpleScheduler + FsrsScheduler (FSRS 21参数)                         |
| **实体层**  | 4/4 Entity    | DeckEntity / NoteEntity / CardEntity / StudySessionEntity                              |
| **数据层**  | 4/4 DB        | DeckDB / NoteDB / CardDB / DBManager 基类                                                |

### 核心功能已实现

- ✅ 五层解耦架构（UI → Manager → Scheduler → Entity → DB）
- ✅ 数据表自动迁移（`_migrate_missing_columns`）
- ✅ 主菜单（统计面板、导航按钮）
- ✅ 牌组管理（树形展示、新建/重命名/移动/归档/删除）
- ✅ 笔记列表（按牌组分组、内嵌编辑面板、新建笔记）
- ✅ 调试面板（完整 CRUD 控制台）
- ✅ 学习会话入口（study 场景 + study_session 场景）
- ✅ 完整学习引擎（SM-2 调度 → 队列组装 → 评分 → 推进 → 统计）
- ✅ 编辑笔记时同步更新关联卡片牌组

## 二、项目结构

### 文件清单

```
res://
├── scenes/
│   ├── ui/                          # UI层 (7个场景)
│   │   ├── main_menu.tscn/gd/md     # 主菜单（入口）
│   │   ├── deck_list.tscn/gd/md     # 牌组管理
│   │   ├── note_list.tscn/gd/md     # 笔记列表
│   │   ├── card_ui.tscn/gd/md       # 卡片显示
│   │   ├── study.tscn/gd/md         # 学习选择页
│   │   ├── study_session.tscn/gd/md # 学习会话
│   │   └── debug_crud_panel.tscn/gd/md # 调试控制台
│   ├── business_logic/              # 逻辑层 (5个脚本)
│   │   ├── manager.gd               # 基类（事务 + 信号）
│   │   ├── deck_manager.gd          # 牌组业务
│   │   ├── note_manager.gd          # 笔记业务
│   │   ├── card_manager.gd          # 卡片业务（完整实现：CRUD + 队列 + 调度落库）
│   │   └── study_manager.gd         # 学习业务
│   ├── data_access/                 # 数据层 (4个脚本)
│   │   ├── db_manager.gd            # 基类（SQLite + Schema）
│   │   ├── deck_db.gd               # 牌组 CRUD + 树
│   │   ├── note_db.gd               # 笔记 CRUD + 搜索
│   │   └── card_db.gd               # 卡片 CRUD + 到期查询
├── src/
│   ├── entities/                    # 实体层 (4个脚本)
│   │   ├── deck_entity.gd
│   │   ├── note_entity.gd
│   │   ├── card_entity.gd
│   │   └── study_session_entity.gd
│   ├── scheduler/                   # 调度层 (2个脚本)
│   │   ├── scheduler.gd             # 抽象基类
│   │   └── simple_scheduler.gd      # 默认实现
│   └── app.gd                       # App 单例（占位）
├── data/
│   ├── db_schema.json              # 数据库表结构定义
│   └── 数据结构.md                   # 数据表设计文档
```

## 三、下一步建议

### 高优先级

1. ~~**实现学习会话完整流程**~~ ✅ 已完成
   
   - StudySession: 取卡 → 显示 → 用户评分 → 调度下一张
   
   - 连接 `StudyManager` 与 `SimpleScheduler`
   
   - 全链路已打通：StudyManager.start_session() → CardManager.get_study_queue() → CardDB.get_all_due_cards()，评分 → SimpleScheduler.calculate_next_state() → CardDB.record_review()

2. **实现 App Autoload 单例**
   
   - 将 `src/app.gd` 改为真正的 Autoload（需在 `project.godot` 中注册）
   
   - 统一管理 Manager 生命周期，避免每个场景自建实例
   
   - 暴露 `get_deck_manager()` / `get_note_manager()` 等全局方法

3. **搜索功能**
   
   - note_list 添加搜索框（搜索栏已有占位节点）
   
   - 调用 `NoteManager.search_notes()`
   
   - 可选：增加牌组过滤下拉

### 中优先级

5. **牌组编辑器（deck_editor.tscn）**
   
   - 当前 deck_list 编辑面板是内嵌的
   
   - 未来可分离为独立场景，支持批量设置

6. **笔记类型系统**
   
   - 创建 note_types 表（支持自定义字段模板）
   
   - 笔记创建时选择模板类型，动态生成字段编辑器

7. **标签系统**
   
   - tags 列落库（当前 is_archived 预留）
   
   - tags 筛选 + 标签云

8. **牌组归档 UI**
   
   - deck_list 编辑面板支持归档/恢复
   
   - 主菜单统计总牌组数 vs 活跃牌组数

### 低优先级

9. **UI 润色**
   
   - BBCode 动画标题
   
   - 过渡动画（场景切换、面板展开）
   
   - 主题切换

10. **性能优化**
    
    - 全量 get_all_notes → `get_notes_count`（计数场景）
    
    - 分页加载（大量笔记时）

11. **设置面板**
    
    - 学习配置（每日新卡数、复习上限等）
    
    - 数据导入/导出

---

## 四、已知问题

- ✅ FSRS 调度器已完成（FsrsScheduler），study.gd 已切换到 FSRS
- `App` 单例尚未注册为 Autoload，每个 UI 场景自建 Manager 实例
- 已有数据库中 deck_id=0 的旧笔记需手动迁移
